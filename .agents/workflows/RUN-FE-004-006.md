# RUN-FE-004-006 — Compred / ind_offers / service offers workflow

## Recommended order

```text
1. FE-004 — Compred.vue practical refactor
2. FE-005 — ind_offers rendering
3. FE-006 — service offers rendering
```

## Why this order

```text
FE-004 separates route shell from orchestration.
FE-005 uses the new/clearer orchestration boundary for flat proposal data.
FE-006 is separate and can be run later or in parallel if working tree is clean.
```

## Independence note

FE-006 may be executed independently from FE-004/005 because it targets services/category pages.

FE-005 may be executed without FE-004 only if the agent first confirms the current `Compred.vue` boundary and keeps changes minimal.

## Commands

Allowed:

```bash
npm run test:run
```

Forbidden by default:

```bash
npm run build
```

## Human launch helpers

### FE-004

```powershell
cd C:\OSPanel\home\websolutions
Get-Content .agents\tasks\LAUNCH-FE-004-compred-practical-refactor.md -Raw | Set-Clipboard
```

### FE-005

```powershell
cd C:\OSPanel\home\websolutions
Get-Content .agents\tasks\LAUNCH-FE-005-ind-offers-rendering.md -Raw | Set-Clipboard
```

### FE-006

```powershell
cd C:\OSPanel\home\websolutions
Get-Content .agents\tasks\LAUNCH-FE-006-service-offers-rendering.md -Raw | Set-Clipboard
```

## Review after each run

Check:

```bash
git status --short
```

Expected each task to create/update its own report:

```text
.agents/reports/REPORT-FE-004-compred-practical-refactor.md
.agents/reports/REPORT-FE-005-ind-offers-rendering.md
.agents/reports/REPORT-FE-006-service-offers-rendering.md
```

If a task modifies unrelated files, pause and review before launching the next task.

